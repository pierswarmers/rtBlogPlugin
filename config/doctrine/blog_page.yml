rtBlogPage:
  actAs:
    rtSearchTemplate:
      fields:      [title, content, description, author_name, tags_as_string]
  columns:
    author_name:      string(255)
    author_email:     string(255)
  inheritance:
    type:              concrete
    extends:           rtPage
